title: HALOS
description: Human–Agent Lineage and Origin Standard
baseurl: ""  # empty for custom domain halos.northharbor.dev (served at root)
theme: minima
sass:
  quiet_deps: true
  silence_deprecations:
    - import
collections:
  journal:
    output: false
  examples:
    output: true
    permalink: /examples/:slug/

defaults:
  - scope:
      path: ""
    values:
      layout: default
