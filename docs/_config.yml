title: HALOS
description: Human-Agent Living Operating System
baseurl: ""  # empty for custom domain halos.northharbor.dev (served at root)
theme: minima
sass:
  quiet_deps: true
  silence_deprecations:
    - import
defaults:
  - scope:
      path: ""
    values:
      layout: default
